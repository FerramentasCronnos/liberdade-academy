# =============================================================================
# App Expo — Liberdade Academy
# Copie para .env na raiz do app. NÃO coloque aqui chaves de TikTok/banco.
# Tudo sensível fica no backend (Portainer). Ver docs/INTEGRATIONS.md
# =============================================================================

# OBRIGATÓRIO pra app falar com o backend real
# Exemplos:
#   http://192.168.0.10:8080          (mesmo Wi‑Fi / IP do VPS)
#   https://api.liberdade.academy     (produção com domínio)
EXPO_PUBLIC_API_URL=

# Sem EXPO_PUBLIC_API_URL o app roda em modo DEMO (AsyncStorage + mocks).
